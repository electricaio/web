import styled from 'styled-components';
import { Card } from 'antd';

export const Header = styled(Card as any)`
  text-align: left;
  font-weight: 400;
  font-size: 16px;
  &.ant-card {
    margin-bottom: 20px;
  }
`;
